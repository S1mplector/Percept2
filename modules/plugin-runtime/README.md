# Plugin runtime

Shared plugin discovery and lifecycle host used by JVN Runtime and JVN Editor. It provides manifest parsing, semantic-version compatibility, dependency ordering, registration ownership, failure isolation, and diagnostics.

```bash
./gradlew :plugin-runtime:test
```

See [Plugin host reference](../../docs/plugins/host-reference.md).
