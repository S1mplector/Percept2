# testkit

Shared test infrastructure module. Provides common test utilities, fixtures, and helpers used by other modules' test suites.

## Build

```bash
./gradlew :testkit:build
```

This module is a dependency target for test source sets in other sub-projects. It does not contain application code.
