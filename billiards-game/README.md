# billiards-game

Bundled billiards mini-game module. Provides a physics-based billiards table scene demonstrating the 2D physics engine, input handling, and JES scene authoring.

## Usage

Included on the `:runtime` classpath alongside `:demo-game`. Launch via the runtime or editor to access the billiards scene.

```bash
./gradlew :runtime:run
```
